## Packages
reactflow | The main library for building node-based editors
lodash | Utility library for object manipulation (get/set) and data processing

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  mono: ["var(--font-mono)", "monospace"],
  sans: ["var(--font-sans)", "sans-serif"],
}
